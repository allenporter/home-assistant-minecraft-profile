{
    "name": "Home Assistant Custom Component Env",
    "build": {
        "context": "..",
        "dockerfile": "../Dockerfile"
    },
    "remoteUser": "homeassistant",
    "mounts": [
        "source=${localWorkspaceFolder}/script/bashrc,target=/home/homeassistant/.bashrc,type=bind,consistency=cached"
    ]
}
