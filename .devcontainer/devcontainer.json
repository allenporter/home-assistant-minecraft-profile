{
	"name": "Home Assistant Custom Component Env",
	"build": {
		"context": "..",
		"dockerfile": "../Dockerfile"
	},
	"mounts": [
		"source=${localWorkspaceFolder}/config/,target=/data/homeassistant/config,type=bind,consistency=cached",
	],
}
